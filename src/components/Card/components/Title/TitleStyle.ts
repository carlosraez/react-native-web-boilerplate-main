import { Flex } from 'native-base';
import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  title: {
    fontSize: 18, 
    fontWeight: 'bold', 
    marginBottom: 8, 
  },
  listTitle: {
    fontSize: 16, 
    fontWeight: 'bold', 
  },
})