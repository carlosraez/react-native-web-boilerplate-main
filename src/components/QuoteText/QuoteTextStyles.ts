import { StyleSheet } from 'react-native';

export default StyleSheet.create({
      quoteText: {
        color: '#000', 
        fontSize: 21, 
        fontStyle: 'italic',
        marginTop:8,
        marginBottom:24
      },
});